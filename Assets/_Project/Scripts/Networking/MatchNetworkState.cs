using UnityEngine;

namespace ProjectMaidan.Networking
{
    /// <summary>
    /// Photon Fusion NetworkObject holding authoritative match state (score, timer, mana).
    /// PROTOTYPE PHASE: stub only. See NetworkManager.cs.
    /// </summary>
    public class MatchNetworkState : MonoBehaviour
    {
        // TODO (Phase 3): [Networked] properties for territory scores, mana, timer
    }
}
